namespace TWF
{
    /// <summary>
    /// A delegate to use as an extension point to customize the rules of the world outside of the core.
    /// </summary>
    public delegate void WorldRulesCustomizer(WorldRules worldRules);
}
