namespace TWF
{
    /// <summary>
    /// A delegate that returns a tool behavior corresponding to the given behavior modifier.
    /// </summary>
    public delegate IToolBehavior IModifiableToolBehavior(string behaviorModifier);
}
